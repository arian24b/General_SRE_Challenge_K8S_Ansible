apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: application
type: Opaque
stringData:
  # Plain text values - Kubernetes will base64 encode them
  username: "your_db_username"
  password: "your_db_password"
  database: "your_db_name"
